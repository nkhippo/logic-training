# legacy/

Phase 2-2（React 化）以前の Vanilla JS フロントエンド資産です。

- `js/` — 旧 JavaScript（参照用）
- `logic.html` / `thinking.html` — 旧 HTML エントリ
- `style.css` — 旧グローバル CSS（React 版は `src/styles/App.css` にコピー済み）

本番は Vite + React（`src/`）を使用してください。GAS 参照は React 版から削除済みです。
