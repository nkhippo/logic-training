# Test PR Label Workflow Trigger

This file exists only to create a safe test pull request for verifying:

- `PR Label Handler` succeeds on `pull_request` events.
- `waiting-for-review` label is automatically added.
