# Issue Bridge Snapshot

- Issue: #241
- Title: 【docs / Important】CLAUDE.md §1（品質基準）を Issue 起票前の必須チェックに組み込み
- URL: https://github.com/nkhippo/ThinkGrindAi/issues/241
- Trigger: issues/labeled
- Updated At (UTC): 2026-05-30T05:56:09Z

## Body

🤖 **Claude より**

## 背景・目的

2026-05-30 の Opus レビューで **Important** 級の問題（観点 A-1-①）として検出。

`CLAUDE.md` §1「運用ドキュメント（要件定義書・仕様書）の品質基準」は**内容そのものは具体的かつ実装可能**だが、**Claude が Issue 起票時に「必ず参照する」導線**が存在しない。

現状の `CLAUDE.md`「#### Issue 起票前の必須チェック」には以下しかない：
- 署名・フォーマット
- タイプ判断
- 分割判断（5軸）
- ready-for-cursor ラベル付与条件（5項目）

→ §1 の「禁止する曖昧さ」「必須記述」を満たしているかのセルフチェックが**ない**ため、Claude が§1 を読まずに Issue 草稿を作る経路が残っている。

「書いてあるだけで読まれない」状態を防ぐため、起票前チェックリストに§1 のセルフチェックを組み込む。

## 実装範囲

### 修正対象ファイル
- `CLAUDE.md`

### 具体的な修正内容

#### `CLAUDE.md`「### Issue 草稿・仕様書を作るときの形式」内「#### Issue 起票前の必須チェック」セクションに、以下のサブチェックを新規追加する

挿入位置：「分割判断（5軸チェック）」の直後、「ready-for-cursor ラベルの付与条件」の直前。

```markdown
**品質基準セルフチェック（タイプ C のみ・仕様書/要件定義書を作成する場合）**

仕様書（`docs/specification*`）または要件定義書（`docs/requirements*`）を作成・更新する場合、以下を起票前に Claude が必ず確認する。1 つでも欠けていれば、起票せず仕様書を作り直す。

**仕様書チェック**
- [ ] §1「禁止する曖昧さ」が含まれていないか
  - 「自然な方法で」「直感的に」「適切に」「API で得られる」「フロントエンドで処理する」「採点が厳しい」等
- [ ] §1「必須記述」がすべて含まれているか
  - 具体的な数値・定数（`max_tokens: 600`、`temperature: 0.3` 等）
  - データ構造の完全定義（JSON 例または TypeScript 型）
  - API パラメータ（呼び出し元ファイル・関数も列挙）
  - 分岐条件（`score >= 95 ならば` のような条件式）
  - プロンプト設計意図（なぜこのパラメータか）

**要件定義書チェック**
- [ ] 技術詳細（実装値・JSON 構造・関数名）が含まれていないか
- [ ] §1「必須記述」がすべて含まれているか
  - なぜこの機能が必要か（背景・課題感）
  - ユーザーが何を体験するか（具体的な UI フロー）
  - 設計判断の理由（複数案から選んだ理由）
  - 制約・非機能要件

**共通チェック**
- [ ] 図表（状態遷移図・API フロー・テーブル）が活用されているか
- [ ] 用語が `docs/TERMS.md` と整合しているか
- [ ] 他ドキュメントへの参照が「ファイル名 §セクション番号」の形式で明示されているか

> ⚠️ 1 つでも欠けていれば、起票前に仕様書・要件定義書を補完する。Cursor の解釈ばらつきを防ぐため、起票後の修正で対応しないこと。
```

#### 補足：CLAUDE.md「### 1. 運用ドキュメント（要件定義書・仕様書）の品質基準」末尾に逆参照を追加

```markdown
> 💡 このセクションは Issue 起票前のセルフチェック（「#### Issue 起票前の必須チェック」内「**品質基準セルフチェック**」）で参照される。
> Issue 起票時には必ずチェックリスト形式で確認すること。
```

## 完了定義

- `CLAUDE.md`「#### Issue 起票前の必須チェック」セクションに「**品質基準セルフチェック（タイプ C のみ）**」サブセクションが存在する状態
- 仕様書チェック・要件定義書チェック・共通チェックの 3 ブロックが含まれている状態
- 仕様書チェックで「禁止する曖昧さ」と「必須記述」が両方カバーされている状態
- 要件定義書チェックで「技術詳細禁止」と「必須記述」が両方カバーされている状態
- §1 末尾に Issue 起票前チェックリストへの逆参照が追加されている状態

## テスト観点

- `grep -n '品質基準セルフチェック' CLAUDE.md` で 2 件ヒット（チェックリスト本体＋§1 末尾の逆参照）
- `grep -n '禁止する曖昧さ' CLAUDE.md` で複数件ヒット（§1 本体＋セルフチェック）
- `grep -n '必須記述' CLAUDE.md` で複数件ヒット（§1 本体＋セルフチェック）
- `grep -n 'TERMS.md' CLAUDE.md` で参照が含まれていること
- 「Issue 起票前の必須チェック」セクション内で、品質基準セルフチェックの挿入位置が「分割判断（5軸チェック）」の直後・「ready-for-cursor ラベルの付与条件」の直前になっていること（目視確認）

## 非対象範囲

- §1 本体（「禁止する曖昧さ」「必須記述」の定義）の修正は対象外
- 「ready-for-cursor ラベルの付与条件」5 項目自体の修正は対象外
- `docs/DOCUMENT_GUIDELINES.md` の修正は対象外
- スキルファイル（`cursor-instruction-writer/SKILL.md`）への連携は対象外（スキル側で対応）
- 仕様書テンプレ・要件定義書テンプレの新規作成は対象外

## Obsidian記録

※ 実装完了後（PR 作成時）、Cursor が以下を Obsidian に保存すること

パス: `/Users/naoya.k/Documents/Obsidian/thinkgrindai/decisions/YYYY-MM-DD-quality-standards-self-check.md`

内容:
```
# CLAUDE.md §1 を Issue 起票前の必須チェックに組み込み
## 実装内容
- 
## 変更ファイル
- 
## 関連Issue
- #XXXX
```

## 作業の進め方

検証が完了したら、確認なしに以下まで一気に進めること：

1. コミット
2. push（未完成でも必ず push すること）
3. PR 作成（Draft 可。**base = `develop`**、ラベル = `docs`、`Closes #XXX` を記載）
   - 未完成の場合はタイトルに `[WIP]` を付けてよい
4. PR 本文（採用版フォーマット）：

---
## 概要
（1〜2 行で要約）

## 変更内容
- 

## 変更理由
- Opus レビューで §1 品質基準が起票時に参照されない Important 問題が検出されたため

## 確認済み事項
- [ ] grep で「品質基準セルフチェック」が複数件ヒットすることを確認
- [ ] チェックリストの挿入位置が指定通りであることを目視確認
- [ ] lint 通過

## 未確認・懸念点
- （あれば記載。なければ「なし」）

## 関連 Issue
Closes #XXX
---

途中で止まってよいのは「不明点がある場合」のみ。その場合は **必ず Issue コメントに以下を書いてから止まること**：

```
【作業中断】
- 現在の状態：
- 中断理由：
- 次に必要なこと：
```

---
_Claude による自動投稿_
